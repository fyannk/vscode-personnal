# Build overlay

Copied into a managed VS Code checkout as `.personal-build/`. Use the standalone project’s `scripts/build-container.sh` or `scripts/build.sh`; do not run these helpers directly from this directory.

Preparation generates `upstream.json` and `build-info.json`. Dependencies and outputs live in the ignored `.work` tree.
